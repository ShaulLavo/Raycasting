{
  "name": "raycast",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "tsc && vite build && gh-pages -d dist"
  },
  "devDependencies": {
    "typescript": "~5.6.2",
    "vite": "^6.0.5",
    "@types/stats.js": "^0.17.3",
    "gh-pages": "^6.3.0"
  },
  "dependencies": {
    "stats.js": "^0.17.0"
  }
}